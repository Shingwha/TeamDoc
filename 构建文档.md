# TeamDoc Lite — 软件构建文档(唯一依据)

> **文档目的**:本文档是重建整个应用的**唯一依据**。执行 Agent 在一个空目录中,只根据本文档从零构建整个应用,不得自行发明文档未描述的功能。文档未覆盖的细节按"最简实现"处理,并在代码注释中标注。
> **产品名**:TeamDoc(Lite 版)
> **定位**:30 人小团队自部署知识库:项目(组)管理文档、实时协同编辑、云空间、全文搜索。
> **语言**:界面与全部文案使用简体中文。

---

## 0. 给执行 Agent 的指引

1. 按 §2 的目录结构创建文件;按 §12 的顺序实现与自测。
2. 严格遵守 §5 数据模型、§7 API 契约、§8 协同协议的字段名与路径(前端依赖它们)。
3. 遇到本文档未定义的细节:选择**最简单、依赖最少**的实现,不做扩展。
4. **禁止引入**:任何前端框架/打包器、NoSQL、消息队列、Docker 之外的部署依赖、TypeScript。
5. 全部完成后跑 §13 验收清单,逐项确认。

---

## 1. 技术栈与总体架构

| 层 | 选型 | 说明 |
|---|---|---|
| 后端 | Python 3.12+ / **FastAPI** + **uvicorn**(单 worker) | 单进程:REST API + WebSocket + 托管静态页 |
| 数据库 | **SQLite**(WAL 模式,busy_timeout 5000) | 文件:`server/data/teamdoc.db`;ORM 用 SQLAlchemy 2.0 |
| 认证 | Cookie 会话 + PAT + TOTP | 密码哈希用 **hashlib.scrypt**(纯标准库,禁止 bcrypt/argon2 原生依赖) |
| 前端 | **纯 HTML + CSS + JavaScript**,无框架、无打包 | 多视图单页壳(§11),CDN 引入 Vditor / Bootstrap 5 / Remix Icon |
| 编辑器 | **Vditor**(CDN 固定版本,即时渲染 ir 模式) | Markdown 编辑 |
| 实时协同 | 原生 WebSocket(Starlette):在线状态 + 内容广播 | 语义:后写者赢(LWW)+ 编辑保护提示(§8) |
| 搜索 | SQLite LIKE(标题+正文/文件名) | 30 人规模足够;严禁引入搜索引擎 |
| 文件存储 | 本地磁盘 `server/data/files/` | 上传用 multipart 流式写盘 |
| CLI | Python 单文件 `cli/td.py`(**纯标准库,零第三方依赖**) | 独立于服务端与前端;PAT + Bearer 直连任意 TeamDoc 实例,操作项目/文档/云空间/搜索,详见 §16 |

**依赖清单**(`server/pyproject.toml`,用 **uv** 管理,**首次不写任何版本约束**,由 uv 解析最新兼容版本并生成 uv.lock):
```toml
[project]
name = "teamdoc-lite"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "SQLAlchemy",
    "pyotp",
    "python-multipart",
]
```

**运行方式**:
```bash
cd lite/server
uv sync            # 自动创建 .venv、按 pyproject 安装依赖并生成 uv.lock
uv run uvicorn main:app --host 0.0.0.0 --port 8000
```
前置:本机已安装 uv(未安装:`pip install uv` 或 `winget install astral-sh.uv`)。
- 端口 8000;必须 `--workers 1`(SQLite 单写者)。
- 首次启动仅自动建表,**不预置任何账号**;无用户时走"初始化向导"(见 §7.1 bootstrap),由首个访问者自建管理员。
- 数据目录 `server/data/`(数据库 + 文件)必须在 `.gitignore` 中。

---

## 2. 目录结构(目标)

```
lite/
├── server/
│   ├── main.py          # FastAPI 入口:挂路由、启动建表/种子、托管静态页
│   ├── models.py        # SQLAlchemy 模型(§5)
│   ├── auth.py          # 密码/会话/PAT/TOTP/权限依赖(§6)
│   ├── docs.py          # 项目/成员/文档树/内容/版本/回收站(§7.3-7.4)
│   ├── files.py         # 云空间:上传/下载/文件夹/归属转移(§7.5)
│   ├── search.py        # 搜索(§7.6)
│   ├── ws.py            # 实时协同 WebSocket(§8)
│   ├── pyproject.toml   # 依赖声明(uv 管理,无版本约束)
│   ├── uv.lock          # uv sync 自动生成,随代码提交
│   └── data/            # 运行时数据(gitignore)
├── cli/
│   └── td.py            # 独立 CLI(§16,阶段 2):PAT 认证 + JSON 信封,纯标准库单文件
└── web/
    ├── index.html       # 唯一入口(SPA 壳)
    ├── css/style.css    # 全部自定义样式(§11.6)
    └── js/
        ├── api.js       # fetch 封装
        ├── app.js       # 路由 + 壳(顶栏/侧栏)+ 登录视图
        └── views/       # projects.js project.js drive.js search.js admin.js settings.js
```

---

## 3. 核心概念与术语

- **项目(Project)**:协作容器,内含模块:**文档** 与 **云空间**(未来扩展:日历等)。成员有角色。
- **角色**:OWNER > ADMIN > EDITOR > VIEWER(数值 3/2/1/0)。全局 `is_admin` 用户在任何项目中视为 ADMIN(可越过 VIEWER 只读限制,但不等于 OWNER,不能删项目,除非本身是 OWNER 成员)。
- **文档(Doc)**:属于某项目,树形(parent_id),内容为 **Markdown 纯文本**,带版本号与历史版本。
- **云空间**:两种作用域:`personal`(个人,仅本人可见)与 `project`(项目,成员可见)。**个人空间定位 = 私有暂存区**。
- **归属制(文件引用核心规则)**:文档只引用**本项目**的文件;从个人空间把文件用于项目文档时,执行**归属转移**(文件记录改挂到项目空间,物理文件不复制不移动)。下载权限 = 一条规则:"是否该项目成员(或全局管理员)";个人空间文件仅本人(及管理员)可下载。**不做跨项目引用**。
- **回收站**:文档软删除(deleted_at),子文档级联软删,可恢复。

---

## 4. 环境变量(全部有默认值,可缺省)

| 变量 | 默认 | 说明 |
|---|---|---|
| `PORT` | 8000 | 监听端口 |
| `MAX_UPLOAD_MB` | 2048 | 单文件上传上限 |
| `SESSION_TTL_DAYS` | 7 | 会话有效期 |

---

## 5. 数据模型(SQLAlchemy)

所有主键:`secrets.token_hex(8)`(16 字符十六进制),时间戳 UTC(无时区 naive)。

| 表 | 字段 |
|---|---|
| **users** | id PK, email(unique, index), name(50), password_hash(200), is_admin bool, is_disabled bool, totp_secret(64, nullable), totp_enabled bool, created_at |
| **sessions** | token PK(64hex), user_id FK idx, totp_verified bool, created_at, expires_at(idx) |
| **pats** | id PK, user_id FK idx, name(50), token_hash(64hex unique), scopes(20, 如 "read" 或 "read,write"), last_used_at nullable, revoked_at nullable, created_at |
| **projects** | id PK, name(100), description(Text 默认空), color(9, 默认 #1f6feb), created_by FK nullable, created_at |
| **project_members** | id PK, project_id FK idx, user_id FK idx, role(10: OWNER/ADMIN/EDITOR/VIEWER, 默认 VIEWER), created_at;UNIQUE(project_id, user_id) |
| **docs** | id PK, project_id FK idx, parent_id(20 nullable), title(200 默认"无标题文档"), content(Text 默认空), sort(Float 默认0), version(Int 默认0), created_by, updated_by nullable, created_at, updated_at(onupdate), deleted_at nullable;(project_id, deleted_at) 查询索引 |
| **doc_versions** | id PK, doc_id FK idx, content(Text), label(100 默认""), created_by nullable, created_at |
| **folders** | id PK, name(100), scope(10: personal/project), project_id nullable idx, user_id nullable idx(personal 所有者), parent_id nullable, created_at, deleted_at nullable |
| **files** | id PK, name(255), scope(10), project_id nullable idx, user_id nullable idx, folder_id nullable, mime(100), size(Int), storage_path(300 unique), created_by nullable, created_at, deleted_at nullable |

连接后执行 PRAGMA:`journal_mode=WAL`、`foreign_keys=ON`、`busy_timeout=5000`。

**注意**:外键删除策略在业务层手动处理(SQLite FK 设了 ON,删除父级前先删子表行,见 §7.3 删项目)。

---

## 6. 认证与权限

### 6.1 密码
- `hash_password(plain)`:16 字节随机盐 + `hashlib.scrypt(n=2**14, r=8, p=1, dklen=32)`,存 `salt_hex$digest_hex`。
- `verify_password`:常数时间比较(hmac.compare_digest)。

### 6.2 会话
- 登录成功 → 生成 `secrets.token_hex(32)` 存 sessions 表(TOTP 流程见 6.4),Cookie:`td_sid=<token>; HttpOnly; SameSite=Lax; Path=/; Max-Age=604800`。
- 每次认证:校验未过期 + 用户未禁用;剩余寿命 < 1/2 时滑动续期。

### 6.3 认证依赖(每个 API 共用)
解析顺序:`Authorization: Bearer tdp_...`(PAT)→ Cookie 会话。解析结果挂到请求级状态:user、scopes(会话=["read","write"];PAT 按存储)、totp_verified、session_token/pat_id。
- `current_user`:未认证 → 401 `{"code":"UNAUTHORIZED","message":"未登录"}`。
- **TOTP 门禁**:用户开启 TOTP 且是会话登录且未验证 → 除 `/api/auth/totp/*`、`/api/auth/logout`、`/api/auth/me` 外一律 401 `{"code":"TOTP_REQUIRED"}`。
- `require_write`:非 GET 方法且 PAT scopes 无 write → 403 "令牌缺少 write 权限"。
- 项目权限:`project_role(db, project_id, user)`:成员角色优先,否则 `is_admin → "ADMIN"`,否则 None;`require_project_role(required)` 不满足 → 403 "需要 {required} 及以上权限"。

### 6.4 TOTP(pyotp)
- `POST /api/auth/totp/setup {password}`:校验密码 → 生成 secret 存 user(不启用)→ 返回 `{secret, uri}`(uri = provisioning_uri(name=email, issuer="TeamDoc"))。
- `POST /api/auth/totp/enable {code}`:verify(secret, code, valid_window=1) 通过 → totp_enabled=true,当前会话 totp_verified=true。
- `POST /api/auth/totp/disable {password}`:校验密码 → 清空 secret、totp_enabled=false。

### 6.5 PAT
- 创建(仅 Web 会话,拒绝 PAT 调用):`tdp_` + `secrets.token_hex(24)`;库存 sha256(token);**明文只在创建响应返回一次**。
- scopes:`read` 或 `read,write`;可吊销(revoked_at);每次认证刷新 last_used_at(距上次 >60s 才写)。

### 6.6 用户管理(仅 is_admin)
- 列表 / 创建(email 唯一,重复 409)/ 修改(name、isAdmin、isDisabled、重置密码)/ **最后一名可用管理员保护**:禁用或取消 admin 会导出 409 "系统至少需要一名可用管理员"。
- `POST /api/users/me/password {oldPassword,newPassword}`:本人改密。

---

## 7. REST API 契约

统一约定:
- 前缀 `/api`;成功直接返回 JSON 数据(无信封);**删除类返回 `{"ok": true}`**(204 亦可,前端按 204/ok 兼容);错误用 HTTPException,`detail={"code": "...", "message": "中文文案"}`。
- 校验失败 400 `VALIDATION`;未登录 401 `UNAUTHORIZED`;无权限 403 `FORBIDDEN`;不存在 404 `NOT_FOUND`;冲突 409 `CONFLICT`。
- 以下 ⭐ 表示需要 write 权限(PAT)。

### 7.1 认证
| 方法/路径 | 说明 |
|---|---|
| GET `/api/auth/status`(公开) | `{bootstrapped, dbReady}`(数据库异常时优雅降级 `dbReady:false`;bootstrapped = 系统已有用户) |
| POST `/api/auth/bootstrap`(公开) | **首次初始化向导**:`{email,name,password(≥8)}`;**仅当 users 表为空**时有效(已有用户 → 403 "系统已初始化");创建第一个管理员并直接登录(设置 Cookie),返回 `{user:{...}}` |
| POST `/api/auth/login`(公开) | `{email,password,totpCode?}`;设置 Cookie;返回 `{user:{id,email,name,isAdmin,avatarColor,totpEnabled}}`;未开 TOTP 密码错 → 401 "邮箱或密码错误";已开 TOTP 未给码 → 401 `TOTP_REQUIRED` "该账号已开启两步验证,请提供动态验证码";码错 → 401 "动态验证码错误" |
| POST `/api/auth/logout` | 销毁会话 + 清 Cookie |
| GET `/api/auth/me` | `{user:{...同上}, auth:{via:"web"/"pat", scopes, totpVerified}}` |
| POST /api/users/me/password ⭐ | `{oldPassword,newPassword(≥8)}`;原密码错 403 "原密码错误" |
| POST /api/auth/totp/setup ⭐ / enable / disable | 见 §6.4 |

### 7.2 用户管理(管理员)
| 方法/路径 | 说明 |
|---|---|
| GET `/api/users` | 全部用户 `{id,email,name,isAdmin,isDisabled,createdAt}` |
| POST `/api/users` ⭐ | `{email,name,password(≥8),isAdmin?}`;邮箱重复 409 "该邮箱已被注册" |
| PATCH `/api/users/{id}` ⭐ | 局部更新;最后管理员保护见 §6.6 |

### 7.3 项目与成员
| 方法/路径 | 权限 | 说明 |
|---|---|---|
| POST `/api/projects` ⭐ | 登录 | `{name(必填),description?,color?}`;创建者成为 OWNER |
| GET `/api/projects` | 登录 | 我参与的;`?all=1` 且管理员 → 全部 |
| GET `/api/projects/{id}` | VIEWER | 项目详情 + `myRole` + memberCount/docCount |
| PATCH `/api/projects/{id}` ⭐ | ADMIN | name/description/color |
| DELETE `/api/projects/{id}` ⭐ | OWNER | 先删 doc_versions(docs in project)、docs、members,再删项目 |
| GET `/api/projects/{id}/members` | VIEWER | `[{userId, role, user:{id,email,name,isDisabled}}]` |
| POST `/api/projects/{id}/members` ⭐ | ADMIN | `{email, role}`;用户不存在 404 "用户不存在(需先用管理员账号创建)" |
| PATCH `/api/projects/{id}/members/{userId}` ⭐ | ADMIN | 改角色;最后一个 OWNER 降级 → 409 "项目至少需要一名所有者" |
| DELETE `/api/projects/{id}/members/{userId}` ⭐ | ADMIN | 移除;同上保护 |

### 7.4 文档
| 方法/路径 | 权限 | 说明 |
|---|---|---|
| GET `/api/projects/{id}/docs/tree` | VIEWER | 未删除文档树:`[{id,title,parentId,updatedAt,children:[...]}]`(按 sort, created_at 排序) |
| GET `/api/projects/{id}/docs/trash` | VIEWER | `[{id,title,deletedAt}]`(倒序,≤500) |
| POST `/api/projects/{id}/docs` ⭐ | EDITOR | `{title?,parentId?}`;父文档校验;sort = 同层最大+1 |
| GET `/api/docs/{id}` | VIEWER | `{id,projectId,parentId,title,content,version,updatedAt,createdAt}`;已删/不存在 404 "文档不存在" |
| PATCH `/api/docs/{id}` ⭐ | EDITOR | `{title?,parentId?}`;parentId 变更需校验同项目且不得移入自己子树(409 "不能移动到自己的子文档下") |
| DELETE `/api/docs/{id}` ⭐ | EDITOR | 软删自身+子树,返回 `{removed:N}` |
| POST `/api/docs/{id}/restore` ⭐ | EDITOR | 恢复子树;父级仍在回收站时移到根;返回 `{restored:N}`;不在回收站 → 409 |
| PUT `/api/docs/{id}/content` ⭐ | EDITOR | `{content}`;**内容有变化时先把旧内容存为 DocVersion(label="覆盖前"),版本保留最近 50 条**;version+=1;返回 `{version}` |
| POST `/api/docs/{id}/append` ⭐ | EDITOR | `{content}`;追加到末尾(空内容分隔 `\n\n`);返回 `{version}` |
| GET `/api/docs/{id}/versions` | VIEWER | `[{id,label,createdAt,createdBy}]` 倒序 ≤100 |
| GET `/api/docs/{id}/versions/{vid}` | VIEWER | `{content}` |
| POST `/api/docs/{id}/versions/{vid}/restore` ⭐ | EDITOR | 等价于对该版本内容执行 PUT content |

### 7.5 云空间(归属制)
权限总则:`personal` → 仅本人(管理员可读);`project` → 项目成员(读 VIEWER / 写 EDITOR)。文件夹移动/重命名同其空间写权限。

| 方法/路径 | 说明 |
|---|---|
| GET `/api/files?scope=&project_id=&folder_id=` | `{folders:[{id,name,createdAt}], files:[{id,name,mime,size,createdAt,createdBy:{id,name}}]}`(软删过滤,≤500) |
| POST `/api/files/folders` ⭐ | `{name, scope, projectId?, parentId?}`;父文件夹必须属于同空间 |
| PATCH `/api/files/folders/{id}` ⭐ | 重命名 |
| DELETE `/api/files/folders/{id}` ⭐ | 仅空文件夹;非空 403 "文件夹非空,请先清空内容" |
| POST `/api/files/upload` ⭐ | **multipart**:字段 `file` + 表单字段 `scope`、`projectId?`、`folderId?`、`mime?`;流式写盘到 `data/files/<id>`(逐 1MB 块,超 MAX_UPLOAD_MB → 400 "文件过大"),mime 优先用客户端提供的,否则 octet-stream;建 files 记录 |
| POST `/api/files/{id}/move` ⭐ | **归属转移**:`{scope:"project", projectId, folderId?}`;仅 personal 所有者或原项目 ADMIN 可发起;目标项目需 EDITOR;只改记录字段(物理文件不动) |
| GET `/api/files/{id}/download?inline=1` | 权限见 §3;inline=1 时 `content-disposition: inline`(图片/预览),否则 attachment + `filename*=UTF-8''` 编码;`FileResponse` |
| PATCH `/api/files/{id}` ⭐ | `{name}` 重命名 |
| DELETE `/api/files/{id}` ⭐ | 软删 |

**文档引用格式**(前端写入 markdown):
- 图片:`![名称](/api/files/{id}/download?inline=1)`
- 附件:`[📎 名称](/api/files/{id}/download)`(前端渲染时给指向 `/api/files/` 的链接追加文件图标样式)

### 7.6 搜索
| 方法/路径 | 说明 |
|---|---|
| GET `/api/search?q=&type=all\|docs\|files` | 权限过滤后 LIKE `%q%` 查询:docs = 我参与项目(管理员=全部)的 title/content;files = 项目文件 + 我个人的文件。返回 `{docs:[{id,projectId,title,snippet}], files:[{id,name,projectId}]}`;snippet = content 中首次命中位置前后各 60 字符拼接(前端做高亮);q 为空返回空集 |

---

## 8. 实时协同 WebSocket

**端点**:`/ws/docs/{doc_id}`;浏览器同源自动带 Cookie;服务端从 `websocket.cookies` 认证。

**连接流程**:
1. accept 后认证:未登录 → `close(code=4401)`;文档不存在/已删 → `close(4404)`;角色为 VIEWER → 连接标记 `readonly=True`(其他人正常)。
2. 服务端按 doc_id 维护连接池:`{ws, userId, name, editing:bool}`。
3. 加入/离开时向该文档**所有人**广播 `{type:"presence", users:[{userId,name,editing}]}`(不含任何敏感信息)。

**客户端 → 服务端**:
- `{"type":"content","content":"<markdown>"}`:readonly 连接直接忽略。保存规则:
  1. content 与当前 doc.content 相同 → 仅回 `{type:"saved", version}`。
  2. 不同 → 旧内容存 DocVersion(label="自动"),版本同样只留 50 条;更新 content、version+=1、updated_by;**提交数据库**。
  3. 回发送者 `{type:"saved", version}`;**广播其他连接** `{type:"remote", content, version, by:<用户名>}`。
- `{"type":"presence","editing":true|false}`:更新自身标记并广播 presence。

**客户端行为**(详见 §11.4):本地编辑防抖 500ms 后发 content;收到 remote 时:若编辑器无焦点且无待保存本地改动 → `vditor.setValue(content)`;否则顶部显示提示条「{by} 更新了文档 [加载最新]」,点击后 setValue 并清除提示。

---

## 9. 启动流程(main.py)

1. `create_all()` 建表;**不预置任何账号**(初始化由 §7.1 bootstrap 完成)。
2. 注册路由顺序:**auth → users → projects/docs → files → search → ws → 静态托管**。
3. 静态托管:`app.mount("/", StaticFiles(directory=WEB_DIR, html=True))` —— 必须在所有 API 路由**之后**(FastAPI 按注册顺序匹配,API 路径优先;未匹配的 `/api/*` 会落到静态 404,可接受)。
4. WEB_DIR 通过相对路径定位(`../web`),不存在时启动报错并提示。
5. uvicorn 单 worker;`if __name__ == "__main__": uvicorn.run(...)` 支持直接 `python main.py`。

---

## 10. 前端总体设计

### 10.1 壳与路由(行业通用形态,SPA + hash 路由)
- 唯一 `index.html`;路由表(app.js 内 ~60 行原生实现:监听 hashchange → 渲染对应视图函数):

| 路由 | 视图 | 说明 |
|---|---|---|
| `#/login` | 登录(无壳) | 居中卡片;含初始化判断(status 接口)与 TOTP 码输入 |
| `#/` | 项目首页 | 项目卡片网格 + 新建项目;卡片显示成员数/文档数/我的角色 |
| `#/p/{projectId}` | 重定向到 `#/p/{projectId}/docs` | |
| `#/p/{projectId}/docs/{docId?}` | **文档模块** | 两栏:文档树(左 260px)+ 编辑器(右) |
| `#/p/{projectId}/files` | **云空间模块** | 面包屑 + 工具栏(新建文件夹/上传)+ 列表 |
| `#/drive` | 我的云空间 | 同云空间视图,scope=personal |
| `#/search?q=` | 搜索结果 | 文档(跳转链接)+ 文件(下载链接)两组 |
| `#/admin` | 管理后台(仅管理员) | 用户表格 + 新建/禁用/重置密码/设管理员 |
| `#/settings` | 个人设置 | 修改密码 / 两步验证绑定 / PAT 管理 |

- **壳组成**(登录后所有页面共用):顶栏 48px(左:产品名 TeamDoc;中:全局搜索框,Ctrl+K 聚焦;右:在线用户头像区[仅文档页]+ 头像下拉菜单[个人设置/退出登录])+ 左侧栏 240px(我的云空间、项目列表、分隔线、管理后台[仅管理员]、个人设置)。侧栏当前路由高亮。
- 路由守卫:未登录(调 `/api/auth/me` 401)→ 一律跳 `#/login`。

### 10.2 项目页(模块 Tabs,方案 B)
- 页头:项目名(点击可改,ADMIN)+ 模块 Tabs:`📄 文档` `📁 云空间`(日历为未来 tab 预留)+ 右侧 `⚙` 按钮 → 模态框组:**项目设置**(改名/描述/颜色/删除项目[OWNER])、**成员管理**(列表/按邮箱添加/改角色/移除)、**回收站**(列表/恢复)。
- Tabs 状态写入路由:`#/p/{id}/docs/...`、`#/p/{id}/files`;再次进入项目默认落在上次访问的 tab(localStorage 记忆)。

### 10.3 文档模块(核心页)
- 左栏文档树:递归渲染;项目头部有「+ 新文档」;节点 hover 显示 `+`(新建子文档)与 `⋯`(重命名/删除[进回收站]);点击节点 → 路由切到该文档;当前文档高亮;树空时显示引导按钮。
- 右栏编辑区(未选文档时显示空态):标题输入框(失焦/防抖 600ms PATCH)+ 保存状态文字(`保存中… / 已保存✓`)+ 在线协作者头像(首字圆形、按 userId 哈希取色)+ Vditor 编辑器。
- **Vditor 集成细节**:
  - CDN(jsdelivr 固定版本 3.10.9):`.../vditor@3.10.9/dist/index.min.js` 与 `.../dist/index.css`。
  - `new Vditor('id', { mode:'ir', cache:{enable:false}, height:'calc(100vh - 210px)', counter:{enable:true}, upload:{ handler(files): 自行 fetch POST /api/files/upload(scope=project, projectId, mime),返回 Vditor 约定 `{msg:'',code:0,data:{succMap:{文件名:url}}}`(图片 succMap 的 url 带 `?inline=1`),errFiles:[] } , link:{}, after(): 加载文档内容后 setValue })`。
  - 自动保存:输入防抖 800ms → 若协同已连走 WS content 消息;未连上则降级 PUT `/content`。
  - 打开文档:GET `/api/docs/{id}` → 填标题 + setValue;标题栏显示版本号。
- 历史版本:编辑区右上「历史」→ 模态框列出版本 → 预览(只读 Vditor 或 marked 渲染)→ 「恢复到此版本」。

### 10.4 云空间视图
- 面包屑(根目录/文件夹链,可点击回跳);工具栏:新建文件夹、上传文件(input multiple);两个表格:文件夹(进入/重命名/删除)与文件(预览新窗口 inline / 下载 / 重命名 / 删除 / **归属转移**[personal 空间显示:输入项目名或从我的项目中选择 → POST move])。
- 文件名链接:图片/_pdf 用 `?inline=1` 新窗口打开,其余直接下载。

### 10.5 样式规范
- 字体:`system-ui, "Microsoft YaHei", sans-serif`;主色 `--primary:#3370ff`;侧栏底 `#f5f6f7`;正文色 `#1f2329`;边框 `#e5e6eb`。
- Bootstrap 5.3 CDN(只用于:表格、模态框、下拉菜单、表单控件基础样式,主色覆盖为 #3370ff);图标 Remix Icon 4.x CDN(class 方式);**不使用任何其他 UI 库**。
- 响应式:≥1200px 完整;窄屏侧栏可折叠为图标(简化:隐藏文字)。

### 10.6 api.js 约定
```js
async function api(path, {method='GET', body, raw=false}={})
// 自动加 credentials:'same-origin';body 为对象时 JSON 序列化 + content-type
// 204 → null;响应含 {detail:{code,message}} 时 throw ApiError(code,message)
// 401 且非登录页 → location.hash = '#/login'
```

---

## 11. 非功能要求

- 所有错误文案中文;金额/日期无需本地化。
- 安全:Cookie HttpOnly+SameSite=Lax;下载接口不得无鉴权;markdown 渲染交给 Vditor(其内部已处理 XSS),自渲染处(搜索预览)只输出纯文本。
- 性能:30 人规模,不做缓存层;列表查询一律带 LIMIT(≤500)。
- 日志:uvicorn 默认访问日志 + 启动日志(种子凭据)即可,不建日志系统。

---

## 12. 构建顺序(Agent 执行步骤)

1. 创建目录与 `pyproject.toml`(依赖不写版本约束);`uv sync` 安装。
2. `models.py` → `auth.py` → `docs.py` → `files.py` → `search.py` → `ws.py` → `main.py`。
3. 启动服务,用 curl/Python 脚本完成 **API 自测**(bootstrap 初始化管理员 → 登录 → 建项目 → 邀请成员 → 建文档 → 写内容 → 版本 → 回收站恢复 → 上传/下载 → 搜索 → TOTP 绑定 → PAT + Bearer 访问),全部通过后进入前端。
4. 前端:`css/style.css` → `js/api.js` → `js/app.js`(壳+路由+登录)→ `views/projects.js` → `views/project.js`(树+Vditor+协同,最重)→ `views/drive.js` → `views/search.js` → `views/admin.js` → `views/settings.js`。
5. 浏览器完整过 §13 验收清单。
6. (阶段 2,可与上一步解耦单独交付)实现 `cli/td.py`(§16),并用运行中的服务自测:`auth login` → 各快捷命令 → `api` 透传 → `--dry-run` 无副作用 → read-only PAT 调写接口得 403+hint → 断网/缺参 exit 1/2。

---

## 13. 验收清单(全部手动通过 = 交付)

1. 启动后访问 `http://localhost:8000` → 无用户时显示"初始化:创建管理员账号"页;创建后自动登录进入系统;再次访问登录页为普通登录。
2. 新建项目 → 侧栏出现 → 进入项目页默认"文档" tab。
3. 新建文档 → 输入标题与内容 → 出现"已保存✓" → 刷新页面内容仍在。
4. **协同**:两个浏览器分别登录两个账号打开同一文档 → 双方头像出现;A 输入后 B 空闲时内容自动更新;B 正在编辑时出现"X 更新了文档"提示条。
5. 树操作:新建子文档、重命名、删除(进回收站)、回收站恢复。
6. 云空间:上传 10MB+ 文件成功、下载一致、重命名、文件夹增删;个人空间文件「归属转移」到项目后在项目空间可见,且项目文档可引用下载。
7. 编辑器粘贴图片 → 自动上传 → 项目空间出现该图片 → 文档内可见。
8. 搜索:命中文档标题与正文、命中文件名;无权限的项目不出现在结果。
9. 管理后台:建用户、禁用(被禁用户立即 401)、重置密码;最后管理员保护生效。
10. 个人设置:改密码后旧密码登录失败;TOTP 绑定后登录要验证码,解绑后恢复;PAT 创建后 `curl -H "Authorization: Bearer tdp_xxx"` 可访问 GET 接口,写接口在 read scope 下 403。
11. 版本:覆盖内容后历史版本出现"覆盖前",恢复后内容回滚且当前内容先被快照。
12. 非法访问:A 项目成员访问 B 项目文档 → 404/403;未登录访问 API → 401 信封。
13. (阶段 2)CLI 基本链路:Web 端创建 PAT 后 `td auth login` 保存配置;之后 `td projects +list`、`td docs +get/-o`、`echo 中文内容 | td docs +write <id> --stdin`、`td drive +upload/+download`、`td search` 全部可用;Web 端刷新能看到 CLI 写入的内容(验证 `--stdin` 中文无乱码)。
14. (阶段 2)CLI 契约:成功命令 stdout 输出 `{"ok":true,...}` 且 exit 0;`--dry-run` 打印请求但不产生任何副作用;read-only PAT 调写命令 → stderr `{"ok":false,...}` exit 1 且带 hint;`td api GET /api/auth/me --raw` 输出原始 JSON;TOTP 已开启的账号用 PAT 走 CLI 不被 TOTP 门禁拦截;缺参数 exit 2;服务器不可达 → `type:"network"` exit 1。

---

## 14. 陷阱清单(上一版实现踩过的坑,务必规避)

1. **SPA 回退路由顺序**:FastAPI 中 `mount("/")` 必须放在全部 API 路由之后,否则 API 被 StaticFiles 抢走。
2. **密码哈希**:不要用 bcrypt/argon2(Windows 原生编译坑),用 hashlib.scrypt。
3. **种子脚本**:独立脚本勿混用语言特性(曾把 TS 标注写进 .mjs 导致语法错误);lite 直接在启动时种子,无需独立脚本。
4. **测试脚本发中文**:Windows 终端 GBK 编码会让 curl 中文变乱码入库;测试用英文数据或 Python requests。
5. **204 响应**:客户端 JSON 解析需先判断 204(空 body)。
6. **SQLite 并发**:必须 WAL + busy_timeout + 单 worker;写操作短事务。
7. **Cookie SameSite=Lax**(不是 Strict,否则外链进入丢会话)。
8. **Vditor cache 必须关闭**(cache:{enable:false}),否则 localStorage 缓存旧内容导致"内容不更新"错觉。
9. **前端不要引任何需要构建的依赖**;全部 CDN + 原生。
10. **动态语言类型宽松**:Python 端所有 body 字段取值后做类型/空值校验(字符串 strip、长度截断)。

---

## 15. 非目标与后续路线图(本期一律不做)

- 日历模块(项目页 tab 预留)
- 字符级真协同(pycrdt / Yjs)
- 外网隧道强制两步验证的入口开关(现只做 TOTP 本身)
- MinIO/S3 存储、文件断点续传(tus)、秒传去重
- Office/PDF 在线预览、电子表格/白板协同
- 回收站彻底删除与定时物理清理
- 通知系统、@提及、评论
- 移动端深度适配

---

## 16. 独立 CLI:`td`(阶段 2,Web 端 §13 验收通过后实施)

> 设计参考飞书官方 [larksuite/cli](https://github.com/larksuite/cli),借鉴其三点:统一 JSON 信封、API 通用透传、`--dry-run` 预览。**不采纳**其 OAuth 设备码/交互式登录——TeamDoc 是单实例自部署,鉴权直接复用 §6.5 的 **PAT**(`tdp_` 前缀),因此**服务端零改动**。

### 16.1 定位与形态
- 独立单文件 `lite/cli/td.py`,**纯 Python 标准库**(`argparse`/`urllib.request`/`json`/`pathlib`),零第三方依赖、不需要 uv sync,系统 Python ≥3.12 直接运行:`python td.py ...`(用户可自行复制/别名成 `td`)。
- 一个 CLI 可连**任意 TeamDoc 实例**(`--server` 指向),通过 token(PAT)操作:项目与成员、文档(读/写/追加/版本/回收站)、云空间(个人空间 + 项目空间:列目录/上传/下载/文件夹/归属转移)、搜索、用户管理(管理员)。
- 面向两类使用者:人(表格输出、`--dry-run`)与 AI Agent(JSON 信封 + exit code,天然可作 Agent 工具入口;将来可把同一组命令包成 MCP 工具,本期不做)。

### 16.2 配置与认证
优先级:命令行 flag > 环境变量 > 配置文件 > 默认值。

| 项 | flag | 环境变量 | 默认 |
|---|---|---|---|
| 服务器地址 | `--server` | `TEAMDOC_SERVER` | `http://localhost:8000` |
| PAT token | `--token` | `TEAMDOC_TOKEN` | 配置文件中的 `token` |
| 输出格式 | `--format` | — | `json` |

- 配置文件:`~/.teamdoc/config.json`(Windows 即 `%USERPROFILE%\.teamdoc\config.json`),内容 `{"server":"...","token":"tdp_..."}`;明文保存 token,单用户自部署不做额外加密。
- 认证子命令:
  - `td auth login --server <URL> --token <tdp_...>`:先调 `GET /api/auth/me` 校验(失败则不写配置并按信封报错),通过后写入配置文件,回显身份与 scopes。
  - `td auth status`:显示 server、user(email/name)、via/scopes、token 掩码(前 8 位)。
  - `td auth logout`:清空配置文件中的 `token` 字段。
- **不做**:OAuth、Cookie 会话登录、TOTP 交互输入(PAT 认证本就不受 TOTP 门禁限制,见 §6.3)。PAT 在 Web 端「个人设置」创建(read 或 read,write)。

### 16.3 输出契约(所有命令统一,Agent 只依赖此契约)
- **成功**:stdout 输出 `{"ok":true,"data":<服务端原始 JSON>}`,exit 0;删除类(204 或 `{"ok":true}`)的 data 归一为 `{"ok":true}`。
- **失败**:stderr 输出 `{"ok":false,"error":{"type":"http"|"network"|"usage","status":<HTTP 状态码,无则 null>,"code":"<服务端 detail.code 或状态文本>","message":"<服务端中文文案或本地描述>"}}`,exit 1;本地用法错误(缺参数/参数非法)type=`usage`,exit 2。
- HTTP 403 时统一附 `hint` 字段(如写接口被拒:`"当前 PAT 可能只有 read scope,请在 Web 端创建 read,write 令牌"`);401 附 hint `"token 无效或已吊销,重新 auth login"`。
- `--format table` 仅列表类命令支持(`projects +list`、`docs +tree`、`docs +versions`、`docs +trash`、`drive +ls`、`search`、`users +list`),无数据打印 `(空)`;其余命令忽略该 flag。

### 16.4 命令集(两层)
**第一层:快捷命令**(`+` 前缀,覆盖常用 90% 场景;所有写类命令支持 `--dry-run`):

| 命令组 | 命令(示例) | 对应 API |
|---|---|---|
| 身份 | `td me` | GET /api/auth/me |
| 项目 | `td projects +list [--all]` / `+get <id>` / `+create --name N [--desc D] [--color C]` | §7.3 |
| 成员 | `td projects +members <id>` / `+add-member <id> --email E --role R` / `+set-role <id> <userId> --role R` / `+remove-member <id> <userId>` | §7.3 |
| 文档读 | `td docs +tree <projectId>` / `+get <docId> [-o out.md]` | §7.4;`+get -o` 只把正文写文件,stdout 信封输出 `{"file","bytes","version"}` |
| 文档写 | `td docs +new <projectId> [--title T] [--parent PID]` / `+write <docId> (--file F\|--stdin) [--title T]` / `+append <docId> (--file F\|--stdin)` | §7.4;内容默认从 stdin 读(管道友好);`+write --title` 同时 PATCH 改名 |
| 文档树管理 | `td docs +rename <docId> --title T` / `+move <docId> --parent PID` / `+delete <docId>` / `+restore <docId>` / `+trash <projectId>` | §7.4 |
| 版本 | `td docs +versions <docId>` / `+version <docId> <vid> [-o]` / `+restore-version <docId> <vid>` | §7.4 |
| 搜索 | `td search "<关键词>" [--type docs\|files]` | §7.6 |
| 云空间 | `td drive +ls [--scope personal\|project] [--project PID] [--folder FID]`(默认 personal) / `+mkdir --name N --scope S [--project] [--parent]` / `+upload <本地路径>... --scope S [--project] [--folder]` / `+download <fileId> [-o 名]` / `+mv <fileId> --to-project PID [--folder FID]`(归属转移) / `+rename <fileId> --name N` / `+rm <fileId>` | §7.5;upload 支持多文件逐个处理;download 缺省文件名取 content-disposition |
| 用户(管理员) | `td users +list` / `+create --email E --name N --password P [--admin]` / `+patch <id> [--name N] [--admin true\|false] [--disabled true\|false] [--password P]` | §7.2 |

**第二层:API 透传**(兜底覆盖未做成快捷命令的全部端点,避免逐个端点写命令):
```bash
td api <METHOD> </api/...> [--data '<JSON 请求体>'] [--raw] [--dry-run]
td api GET /api/projects/p1/members
td api PATCH /api/files/f9 --data '{"name":"新名字"}'
```
- path 必须以 `/api` 开头,自动附加 Bearer;支持 GET/POST/PATCH/PUT/DELETE;`--raw` 直接输出原始响应体(不包信封,便于管道给 jq/python)。

### 16.5 实现约束
- HTTP 用 `urllib.request`,超时 30s,网络异常 → `type:"network"`;请求与响应**显式 UTF-8**(Windows GBK 坑见 §14.4,`--stdin` 必须读 `sys.stdin.buffer` 再 decode)。
- multipart 上传手写 boundary(`uuid4`),文件按块读入;大小由服务端 MAX_UPLOAD_MB 兜底校验。
- 下载流式写盘(1MB 块),结束时输出一行 `已下载 N 字节 → 路径`(stderr,不影响信封)。
- `--dry-run`:打印 `DRY-RUN <METHOD> <完整URL> body=<JSON>` 后直接 exit 0,不发请求。
- PAT scopes 为 read 时,写命令不做本地预判,把服务端 403 信封透出即可(附 §16.3 的 hint)。
- 不做自动翻页(服务端列表本就 LIMIT ≤500 一次返回)、多 profile、shell 补全、进度条。

### 16.6 CLI 非目标
OAuth/浏览器交互登录、MCP server 包装、插件体系、批量双向同步(git 式 pull/push)、Office/PDF 导出。

---

## 17. 变更记录

| 日期 | 决策 |
|---|---|
| 2026-09-09 | 初版:方案 B(项目页模块 Tabs)、归属制文件引用、LWW 协同、FastAPI+SQLite+纯静态前端定稿 |
| 2026-09-09 | 新增 §16:独立 CLI `td`(单文件纯标准库,PAT 认证 + JSON 信封 + API 透传 + `--dry-run`,设计参考飞书官方 larksuite/cli);列为阶段 2,服务端零改动,§12/§13 已同步 |
